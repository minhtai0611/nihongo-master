# Build artifacts — regenerated by the pipeline, never committed.
# (ir.json is a pure function of the baseline PDF and extract.py.)

tools/ir.json
tools/book.pdf

__pycache__/
*.pyc

# Third-party font binaries — fetched by tools/getfonts.py, never committed.
tools/fonts/
